{
  "name": "breathe-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "web-ext run",
    "dev:chrome": "web-ext run -t chromium",
    "lint": "web-ext lint",
    "build": "web-ext build --overwrite-dest"
  },
  "devDependencies": {
    "web-ext": "^10.6.0"
  }
}
